name: GoogleContainers
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Login in Docker Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCRIOTOKEN }}
      - name: Docker pull and push
        run: |
          docker pull k8s.gcr.io/kube-cross:latest
          docker tag  k8s.gcr.io/kube-cross:latest  ghcr.io/khh403/kube-cross:latest
          docker push ghcr.io/khh403/kube-cross:latest
